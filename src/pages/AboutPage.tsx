export default function AboutPage() {
    return (<section className="card">
      <h1>About</h1>
      <p className="muted">
        Esta práctica sirve para aprender React + TypeScript consumiendo una API y evolucionarla con auth y rutas protegidas.
      </p>
    </section>);
}