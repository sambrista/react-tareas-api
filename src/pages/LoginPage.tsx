export default function LoginPage() {
    return (<section className="card">
      <h1>Login</h1>
      <p className="muted">En la siguiente iteración conectaremos este formulario a la API con JWT.</p>
    </section>);
}